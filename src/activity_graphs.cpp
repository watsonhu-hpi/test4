#include "../include/activity_graphs.hpp"
#include <iostream>

void ActivityGraphs::generateGraph(const std::string& username) {
    std::cout << "Generating activity graph for user: " << username << std::endl;
    // Simulate logic to generate a user activity graph
}