#include "../include/activity_heatmap.hpp"
#include <iostream>

void ActivityHeatmap::generateHeatmap(const std::string& username) {
    std::cout << "Generating activity heatmap for user: " << username << std::endl;
    // Simulate logic to generate a user activity heatmap
}